{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate deploy",
    "dev": "npm run generate && uvicorn app.main:app --reload --port 5000",
    "build": "npm run generate && npm run migrate",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 5000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "prisma": "^5.17.0"
  },
  "dependencies": {
    "@prisma/client": "^5.17.0"
  }
}
